# Default install remains CPU for local dev and non-GPU runtimes.
-r requirements.cpu.txt

# For GPU workers use requirements.gpu.txt (onnxruntime-gpu).
# torch and ultralytics are build-time only (ONNX export) — not installed at runtime.
